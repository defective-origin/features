// - TODO: implement rest api + yup validator   .validate(requestDataYup, responseDataYup)  what todo with meta.Pagination
// - TODO: add tests
// - TODO: Add new Type approach for javascript
// example:
// type amount = Yup.number().min(-999999).max(999999).nullable()
// type uuid = Yup.uuid().required()

// - Board wrapper
// - Widget wrapper - without card | by url
// - Context and useContext creator with types - [Context, useContext] = createContext, value.update()





export const TEXT =  'DEFAULT_TEXT'
